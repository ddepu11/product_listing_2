import styled from "styled-components";

const Cart = () => {
  return (
    <Wrapper>
      <h2>Cart</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Cart;
